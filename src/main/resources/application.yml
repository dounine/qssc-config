server:
  port: 8888
  
logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'INFO'

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dounine/ls
          basedir: target/config